Орлов Михаил Федорович Р3123 504537

Отчет по лабораторной работе:

1. Программа построена логично, по модулям. Главные функции (guess_number, input_helper) отделены от вспомогательных алгоритмов (_binary_search, _linear_search). Старался писать чистый код.

2. Функция input_helper() проверяет, что вводит пользователь:

-  Диапазон чисел задан верно (начало меньше или равно концу).
- Число находится в заданном диапазоне.
- Если выбран неверный метод поиска, по умолчанию ставится на бинарный.

3. В программе предусмотрены разные ситуации:

- Можно выйти из игры с помощью Ctrl+C (KeyboardInterrupt). #этот класс исключения мне подсказал deepseek
- Если пользователь вводит что-то не то, программа это обрабатывает (ValueError).
- Для бинарного поиска есть защита от пустых или несортированных диапазонов.

4. Юниттесты проверяют следующие случаи:

- Поиск чисел в начале, конце и середине диапазона.
- Работа с пустыми и маленькими диапазонами.
- Обработка чисел вне диапазона.
- Автоматическое исправление метода поиска.
- Проверка, что бинарный поиск работает только с отсортированными данными.
- Проверка первого и последнего элемента.

5. Код оформлен по стандарту PEP-257, есть подробные пояснения (docstrings) и указаны типы данных для лучшего понимания.

6. Сделал понятный "интерфейс" игры на русском языке. Сообщения об ошибках ясны, а результаты работы алгоритмов подробные.
